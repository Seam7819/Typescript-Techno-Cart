// Reference type object 

// This is called explicit data type when we tell the type 
const user: {
    name: "Sultan";  // this is literel data type. This is also a type is ts. 
    readonly age: number; //this also cannot be changed because it is also a readonly prop
    Institute: string;
} = {
    name : "Sultan",
    age : 26,
    Institute : "Green University"
}

